#identerd is the id entered by user
	x=hashing_method2(qentered+ansentered)
	us2=w^hashing_method1(pwentered+x)
	rpw=hashing_method1(identered+pwentered+us2)
	vs2=hashing_method1(identered+rpw+x)
	
	if(vs2==v):#v yu receivers from server1
		xuentered=yu^rpw
		a=randint(0,9999999999999999999)
		c1=a*g
		#g from server1
		cux=a*PublicKey
		cuy=
		DIDU=identered^cux
		tuentered=int(time.time())

		mu=hashing_method1(identered+tuentered+xuentered+cu+c1)
		server1(DIDU,mu,c1.tuentered)
		#control passed to server1

		ts1=int(time.time())
		deltats2=ts1-ts#ts sent from server 1
		if(deltats2<=7):
		
			temp=a*c2
			if((sk==hashing_method1(identerd+xuentered+tu+temp+cu))&&(ms==hashing_method1(sk+tu+ts+xuentered+identered))
			print("Authorised Server")
			else
			print("Unauthorised Server")	
			#cu?? cux cuy
		
		auth=hashing_method1(identered+xuentered+realm+sk+ts)
		response=server1(realm,auth)

	
	else:
		print("Wrong information entered")
	#code for exiting
	